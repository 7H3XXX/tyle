# Vercel Blob (created automatically when a Blob store is connected to the Vercel project)
BLOB_READ_WRITE_TOKEN=

# Admin dashboard (/admin) — HTTP Basic auth. Required in production.
ADMIN_USER=admin
ADMIN_PASSWORD=

# Optional: use the local file store with `next start` (never on Vercel)
# STORAGE_DRIVER=local
